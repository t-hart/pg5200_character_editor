<Window x:Class="CharacterEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        Height="300"
        Width="556.787"
        Title="MVVM Light Application"
        DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="LayoutRoot">

        <TextBlock FontSize="36"
                   FontWeight="Bold"
                   Foreground="Purple"
                   Text="{Binding WelcomeTitle}"
                   VerticalAlignment="Bottom"
                   TextWrapping="Wrap" Margin="17,19,19,196.667" Width="514" Height="55" HorizontalAlignment="Center" />
        <TextBox HorizontalAlignment="Left" Height="23" Margin="117,188,0,0" TextWrapping="Wrap" Text="{Binding Strength.Value, Mode=TwoWay}" VerticalAlignment="Top" Width="84"/>
        <Button Content="Reset" HorizontalAlignment="Left" Margin="226,187,0,0" VerticalAlignment="Top" Width="75" RenderTransformOrigin="0.513,0.547" Command="{Binding ResetCommand, Mode=OneWay}" CommandParameter="{Binding Strength, Mode=OneWay}"/>
        <Button Content="Increment" HorizontalAlignment="Left" Margin="226,221,0,0" VerticalAlignment="Top" Width="75" Command="{Binding IncrementCommand, Mode=OneWay}"/>
        <TextBox HorizontalAlignment="Left" Height="23" Margin="132,140,0,0" TextWrapping="Wrap" Text="{Binding Test}" VerticalAlignment="Top" Width="120"/>

    </Grid>
</Window>
